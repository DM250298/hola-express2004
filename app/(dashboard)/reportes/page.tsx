import { PantallaReportes } from '@/components/reportes/PantallaReportes'

export const metadata = {
  title: 'Reportes — ¡Hola! Express',
}

export default function PaginaReportes() {
  return <PantallaReportes />
}
